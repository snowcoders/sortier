version: 2.1
orbs:
  node: circleci/node@3.0.0
jobs:
  test:
    executor: node/default
    steps:
      - checkout
      - node/install-packages
      - run: npm run test
workflows:
  test:
    jobs:
      - test
